var gulp = require('gulp');
var recombiner = require('cortex-recombiner');

gulp.task('cortex2npm', function() { 
    //重组cortex 包结构，使得其兼容webpack
    recombiner({
        base: __dirname, //项目根目录
        noBeta: false //忽略neurons文件夹下beta版本的cortex包，如果开启此项功能，则必须保证neurons下所有的包都含有非beta版本
    });
});

