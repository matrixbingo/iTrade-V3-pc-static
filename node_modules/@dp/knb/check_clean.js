var execSync = require('child_process').execSync;
var hasChanges = String(execSync('git diff --name-only')).trim();
if (hasChanges) throw new Error("ERROR: There are files not added:\n----\n" + hasChanges + "\n----\n");
